{
  "name": "kamilla-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "sass-embedded": "^1.86.0",
    "vite": "^6.2.3"
  },
  "dependencies": {
    "framer-motion": "^12.23.13",
    "lenis": "^1.3.11"
  }
}
